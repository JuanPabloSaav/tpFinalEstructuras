package test;
import estructuras.conjuntistas.TablaHash;
import estructuras.lineales.Lista;
import objetos.Ciudad;
import objetos.Equipo;

public class testTablaHash {
    /* public static void main(String[] args) {
        insertarTest();

    }

    public static void insertarTest(){
        TablaHash tabla = new TablaHash(10);
        Partido partido1 = new Partido(new Equipo("flopa", "A", "peru", 0, 0, 0), new Equipo("unflopa", "A", "bolivia", 0, 0, 0), new Ciudad("lima", "peru", true, false), 0, 0, 1);
        tabla.insertar(partido1);
        for (int i = 0; i < 10; i++) {
            Partido partido = new Partido(new Equipo("apellido "+ i, "A", "pais" + i, 0, 0, 0), new Equipo("apellido" + (i+1), "A", "pais" + (i+1), 0, 0, 0), new Ciudad("ciudad" + i, "pais" + i, true, false), 0, 0, 1);
            tabla.insertar(partido);
        }

        System.out.println(tabla.toString());
        Lista lista = tabla.listar();
    } */
}
